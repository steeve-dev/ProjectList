{% extends 'base.html.twig' %}
{% block body %}
    <a href="{{ path('ListPage') }}">retour aux listes</a>


<h1>{{ list.name }}</h1>
    {{ form_start(form) }}
    {{ form_row(form) }}
    <button>valider</button>
    {{ form_end(form) }}
<ul>
    {% for taskList in list.tasks %}

        <li>
            {{ taskList.name }}
        </li>

    {% endfor %}

</ul>




{% endblock %}