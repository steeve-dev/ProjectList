{% extends 'base.html.twig' %}

{% block body %}

    <h1>Mon compte</h1>

    {{ form_start(form) }}
    {{ form_row(form) }}
    <button>Modifier</button>
    {{ form_end(form) }}





{% endblock %}